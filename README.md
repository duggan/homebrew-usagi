# homebrew-usagi

Homebrew tap for [usagi](https://github.com/duggan/usagi) — a minimalist Claude usage tracker for the macOS menu bar.

```sh
brew tap duggan/usagi
brew install --cask usagi
```

`Casks/usagi.rb` is bumped automatically by usagi's release workflow.
